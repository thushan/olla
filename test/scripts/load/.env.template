# Olla Load Test Configuration (template)

# Target Olla proxy URL (no trailing slash)
TARGET_URL=http://localhost:40114

# Rate limiting - delay between requests in seconds (0 for no limit)
RATE_LIMIT=0.1

# Model name to test with
MODEL_NAME=phi4:latest